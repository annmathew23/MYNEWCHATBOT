APP_NAME=Chat Platform
JWT_SECRET=change-me
JWT_ALG=HS256
JWT_EXPIRE_MIN=60

# SQLite (dev)
DATABASE_URL=sqlite:///./chat.db

# OR Postgres (prod)
# DATABASE_URL=postgresql+psycopg://postgres:password@db:5432/chat

# Providers (use at least one or use provider=mock)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3.1-8b-instruct

GROQ_API_KEY=
